[package]
name = "oracle_consensus"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
test = true

[dependencies]
soroban-sdk.workspace = true

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }

[features]
default = []
test = ["soroban-sdk/testutils"]
